# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Android signing — NEVER commit keystores or their credentials
*.jks
*.keystore
keystore.properties
key.properties

# Updater signing — NEVER commit the private key (only the .pub is safe)
updater-signing.key
*.updater.key
TAURI_SIGNING_PRIVATE_KEY*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
